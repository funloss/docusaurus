{
  "name": "my-notes",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prestart": "rm -rf docs && git clone --depth 1 --filter=blob:none --sparse https://github.com/funloss/funKnowledge.git temp-docs && cd temp-docs && git sparse-checkout set 笔记 && cd .. && mv temp-docs/笔记 docs && rm -rf temp-docs && node scripts/fix-br-tags.js",
    "prebuild": "rm -rf docs && git clone --depth 1 --filter=blob:none --sparse https://github.com/funloss/funKnowledge.git temp-docs && cd temp-docs && git sparse-checkout set 笔记 && cd .. && mv temp-docs/笔记 docs && rm -rf temp-docs && node scripts/fix-br-tags.js",
    "docusaurus": "docusaurus",
    "start": "docusaurus start",
    "build": "docusaurus build",
    "swizzle": "docusaurus swizzle",
    "deploy": "docusaurus deploy",
    "clear": "docusaurus clear",
    "serve": "docusaurus serve",
    "write-translations": "docusaurus write-translations",
    "write-heading-ids": "docusaurus write-heading-ids"
  },
  "dependencies": {
    "@docusaurus/core": "2.4.3",
    "@docusaurus/preset-classic": "2.4.3",
    "@mdx-js/react": "^1.6.22",
    "clsx": "^1.2.1",
    "prism-react-renderer": "^1.3.5",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@docusaurus/module-type-aliases": "2.4.3"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=16.0"
  }
}
